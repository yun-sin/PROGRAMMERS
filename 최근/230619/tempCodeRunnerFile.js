  // console.log(genreMap);
  // console.log(
  //   [...genreMap.entries()].sort((a, b) => b[1].total - a[1].total)
  //   //.flatMap((item) => item[1].songs)
  //   // .map((song) => song.i)
  // );